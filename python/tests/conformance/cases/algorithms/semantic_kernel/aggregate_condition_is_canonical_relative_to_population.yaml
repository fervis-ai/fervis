id: aggregate_condition_is_canonical_relative_to_population
kind: algorithm
algorithm: semantic.kernel
input:
  mode: relative_qualification
  requested_fact_id: fact_1
  given:
    kind: atom
    ref: during_period
  condition:
    kind: and
    arguments:
      - kind: atom
        ref: approved
      - kind: atom
        ref: during_period
expect:
  result_contains:
    residual_clauses:
      - [approved]
