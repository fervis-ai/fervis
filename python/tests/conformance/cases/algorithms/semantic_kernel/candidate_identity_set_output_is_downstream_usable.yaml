id: candidate_identity_set_output_is_downstream_usable
description: A qualifying-row result can return its candidate set identity to downstream planning.
kind: algorithm
algorithm: semantic.question_contract
input:
  question_context_texts:
  - Which staff member is named Nadia Wanjiku?
  frame_payload:
    decision_basis: The answer returns the qualifying staff member identity.
    outcome:
      kind: question_meaning
      answer_requests:
      - return_request_basis: Return the staff member named Nadia Wanjiku.
        relational_shape_basis: The requested relational shape is ordinary.
        request:
          relational_shape: ordinary
          result_grain_basis: The requested result grain is one_result_per_qualifying_row.
          result:
            kind: one_result_per_qualifying_row
            result_candidates:
              instance_kind: staff member
              origin:
                kind: question
            projection:
              projection_basis: Return the qualifying staff member identity.
              candidate_identity: returned
              explicitly_requested_values: []
            result_order:
              ordering_request_basis: The question requests no ordering.
              ordering:
                kind: no_ordering_requested
              selection:
                kind: all_results
      supplied_values:
        selection_limits: []
        operands:
        - meaning: Nadia Wanjiku
          denotation_basis: Nadia Wanjiku names one staff member.
          entity_reference:
            instance_kind: staff member
            value:
              kind: single_identity
              identity_value: Nadia Wanjiku
              origin:
                kind: question
      question_input_inventory_check:
        all_input_like_phrases_declared: true
  payload:
    decision_basis: Return the staff candidate whose identity matches Nadia Wanjiku.
    outcome:
      kind: question_contract
      answer_requests:
      - requested_fact_ref: fact_1
        origin:
          source: question_context
          meaning: staff member named Nadia Wanjiku
          resolved_input_ref: null
        candidate_set:
          instance_kind: staff member
          instance_interpretation: normal_business_instance
        set_graph:
          identity_input_relations:
            i1: null
          requested_output_relations: {}
          other_related_sets: []
        qualification:
          kind: input_comparison
          input:
            kind: input_ref
            input_ref: i1
            operand_meaning: Nadia Wanjiku
            instance_kind: staff member
          fact:
            kind: fact
            identity_path:
              kind: candidate_instance
            origin:
              source: question_context
              meaning: staff member identity
              resolved_input_ref: null
          operator: equals
        grouping: []
        ordering: []
        selection: null
        distinct_by: []
        outputs:
          result_key_outputs:
          - expression:
              kind: set_ref
              set_ref: s1
          requested_value_outputs: []
expect:
  result_contains:
    requested_facts:
    - id: fact_1
      result_grain: subject_rows
      output_refs:
      - fact_1:output:output_1
