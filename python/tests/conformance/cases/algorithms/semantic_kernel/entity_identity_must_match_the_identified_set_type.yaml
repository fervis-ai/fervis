id: entity_identity_must_match_the_identified_set_type
description: A supplied person identity cannot be compared with a shift candidate identifier.
kind: algorithm
algorithm: semantic.question_contract
input:
  question_context_texts:
  - Which shifts were worked by Nadia Wanjiku?
  frame_payload:
    decision_basis: Return shift occurrences related to the supplied person.
    outcome:
      kind: question_meaning
      answer_requests:
      - return_request_basis: Return the shifts worked by Nadia Wanjiku.
        relational_shape_basis: The supplied person qualifies related shift rows.
        request:
          relational_shape: ordinary
          result_grain_basis: One result row represents one qualifying shift.
          result:
            kind: one_result_per_qualifying_row
            result_candidates:
              instance_kind: shift
              origin:
                kind: question
            projection:
              projection_basis: Return each qualifying shift identity.
              candidate_identity: returned
              explicitly_requested_values: []
            result_order:
              ordering_request_basis: The question requests no ordering.
              ordering:
                kind: no_ordering_requested
              selection:
                kind: all_results
      supplied_values:
        selection_limits: []
        operands:
        - meaning: Nadia Wanjiku
          denotation_basis: Nadia Wanjiku names the person related to qualifying shifts.
          answer_request_numbers: [1]
          entity_reference:
            instance_kind: person
            value:
              kind: single_identity
              identity_value:
                kind: literal
                value: Nadia Wanjiku
              origin:
                kind: question
      question_input_inventory_check:
        all_input_like_phrases_declared: true
  payload:
    decision_basis: Incorrectly compare Nadia Wanjiku with each shift identity.
    outcome:
      kind: question_contract
      answer_requests:
      - requested_fact_ref: fact_1
        origin:
          source: question_context
          meaning: shifts worked by Nadia Wanjiku
          resolved_input_ref: null
        candidate_set:
          instance_kind: shift
          instance_interpretation: resource_population
        set_graph:
          identity_input_relations:
            i1: null
          requested_output_relations: {}
          other_related_sets: []
        qualification:
          kind: input_comparison
          input:
            kind: input_ref
            input_ref: i1
            operand_meaning: Nadia Wanjiku
            instance_kind: person
          fact:
            kind: fact
            identity_path:
              kind: candidate_instance
            origin:
              source: question_context
              meaning: shift identity
              resolved_input_ref: null
          operator: equals
        grouping: []
        ordering: []
        selection: null
        distinct_by: []
        outputs:
          result_key_outputs:
          - expression:
              kind: set_ref
              set_ref: s1
          requested_value_outputs: []
expect:
  result_contains:
    status: rejected
