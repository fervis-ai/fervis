id: empty_aggregate_with_carry_field_fails_closed
description: Empty complete aggregate with a carry field returns a controlled error.
migrates:
  - api/tests/fervis/lookup/plan_execution/test_relation_engine_operations.py::test_empty_complete_aggregate_with_carry_field_returns_controlled_error
kind: algorithm
algorithm: execution.relation_engine
input:
  relations:
    - id: items
      rows: []
      grain_keys: [location_id]
      completeness: {status: complete, source_kind: api_read, set_kind: unknown}
  operations:
    - id: count_items
      output_relation: item_count
      spec:
        kind: aggregate
        input_relation: items
        group_by: []
        carry_fields:
          - {source: label}
        aggregations:
          - {function: count, output_field: count}
expect:
  result_contains:
    status: rejected
