id: prior_answer_request_projects_subject_neutral_count_output
description: Prior request memory separates a retained row-count operation from its replaceable subject.
kind: algorithm
algorithm: memory.prior_answer_request
input:
  source_question: How many locations do we have?
  subject_meaning: location
  output_meaning: number of locations
expect:
  result_contains:
    frame:
      parts:
        - part_id: subject
          kind: subject
          text: location
        - part_id: output:1
          kind: requested_output
          text: row count
          value_type: integer
