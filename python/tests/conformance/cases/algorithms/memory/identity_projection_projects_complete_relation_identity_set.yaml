id: identity_projection_projects_complete_relation_identity_set
description: Complete API-read relations project complete typed entity-key sets.
migrates:
  - api/tests/fervis/memory/test_identity_values.py::test_memory_projection_projects_complete_relation_identity_set
kind: algorithm
algorithm: memory.identity_projection
input:
  conversation_context:
    factArtifacts:
      - artifactId: turn_1
        outcome: answered
        addresses:
          - address: relation.sales
            kind: relation
            source: {kind: api_read}
            grainKeys: [sale_id]
            completeness: {status: complete, pagination: all_pages, rowCount: 2}
            rowAddresses: [row.sales.1, row.sales.2]
          - address: row.sales.1
            kind: row
            relation: relation.sales
            identity: {sale_id: sale-1}
            values:
              answer_1:
                type: entity_key
                value: {entityKind: sale, keyId: primary_key, components: {sale_id: sale-1}}
          - address: row.sales.2
            kind: row
            relation: relation.sales
            identity: {sale_id: sale-2}
            values:
              answer_1:
                type: entity_key
                value: {entityKind: sale, keyId: primary_key, components: {sale_id: sale-2}}
expect:
  result_contains:
    identity_sets:
      - id: mem.turn_1.relation.sales.entity_key_set.answer_1
        kind: identity_set
        entity_kind: sale
        key_id: primary_key
        keys:
          - entity_kind: sale
            key_id: primary_key
            components: {sale_id: sale-1}
          - entity_kind: sale
            key_id: primary_key
            components: {sale_id: sale-2}
        count: 2
        display_label: 2 sale identities from prior answer
        display_label_author: backend
        source_relation_id: turn_1.relation.sales
        completeness:
          status: complete
          pagination: all_pages
          row_count: 2
