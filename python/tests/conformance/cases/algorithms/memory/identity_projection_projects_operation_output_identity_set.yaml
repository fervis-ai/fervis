id: identity_projection_projects_operation_output_identity_set
description: Complete operation-output relations project complete typed entity-key sets.
migrates:
  - api/tests/fervis/memory/test_identity_values.py::test_memory_projection_projects_complete_operation_output_identity_set
kind: algorithm
algorithm: memory.identity_projection
input:
  conversation_context:
    factArtifacts:
      - artifactId: turn_1
        outcome: answered
        addresses:
          - address: relation.answer_rows
            kind: relation
            source: {kind: operation_output}
            grainKeys: [sale_id]
            completeness: {status: complete, pagination: not_paginated, rowCount: 2}
            rowAddresses: [row.answer_rows.1, row.answer_rows.2]
          - address: row.answer_rows.1
            kind: row
            relation: relation.answer_rows
            identity: {sale_id: sale-1}
            values:
              answer_1:
                type: entity_key
                value: {entityKind: sale, keyId: primary_key, components: {sale_id: sale-1}}
          - address: row.answer_rows.2
            kind: row
            relation: relation.answer_rows
            identity: {sale_id: sale-2}
            values:
              answer_1:
                type: entity_key
                value: {entityKind: sale, keyId: primary_key, components: {sale_id: sale-2}}
expect:
  result_contains:
    identity_sets:
      - id: mem.turn_1.relation.answer_rows.entity_key_set.answer_1
        kind: identity_set
        entity_kind: sale
        key_id: primary_key
        keys:
          - entity_kind: sale
            key_id: primary_key
            components: {sale_id: sale-1}
          - entity_kind: sale
            key_id: primary_key
            components: {sale_id: sale-2}
        count: 2
        display_label: 2 sale identities from prior answer
        display_label_author: backend
        source_relation_id: turn_1.relation.answer_rows
        completeness:
          status: complete
          pagination: not_paginated
          row_count: 2
