id: review_scope_selectable_param_decisions_own_input_tests
description: Source-binding review scope treats selectable param decisions as owners for grounded input membership tests.
kind: algorithm
algorithm: source_binding.review_scope
input:
  request:
    mode: scoped_review_selectable_owned_inputs
expect:
  result_contains:
    finite_choice:
      status:
        - subject_identity
        - normal_instance_guard
    row_predicate:
      rp.source_1.row.root.is_deleted:
        - subject_identity
        - normal_instance_guard
  result_exact_fields:
    finite_choice:
      status:
        - subject_identity
        - normal_instance_guard
    row_predicate:
      rp.source_1.row.root.is_deleted:
        - subject_identity
        - normal_instance_guard
