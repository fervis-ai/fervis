id: row_predicate_parse_unknown_values_do_not_filter
description: Unknown row-predicate effects preserve the complete governed population.
migrates:
  - api/tests/fervis/lookup/test_source_binding_one_call.py::test_source_binding_unknown_row_predicate_review_does_not_filter_rows
kind: algorithm
algorithm: source_binding.row_predicate_parse
input:
  row_predicates:
    is_active:
      "true": UNKNOWN_TEST_EFFECT
      "false": UNKNOWN_TEST_EFFECT
expect:
  result_contains:
    population_choices:
      - controller_kind: row_predicate
        controller_id: rp.source_1.row.root.is_active
        field_id: is_active
        requested_fact_ids: [fact_1]
        included_values: ["true", "false"]
        excluded_values: []
