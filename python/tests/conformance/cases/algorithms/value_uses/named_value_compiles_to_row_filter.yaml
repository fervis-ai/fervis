id: named_value_compiles_to_applied_filter
description: A named value constraint compiles through the canonical applied-filter path.
migrates:
  - api/tests/fervis/lookup/fact_planning/test_values.py::test_named_value_compiles_to_row_filter_when_sink_selects_field
kind: algorithm
algorithm: planning.value_uses
input:
  catalog:
    reads:
      - id: records
        endpoint_name: list_records
        fields:
          - ref: field.data.group_label
            path: data.group_label
            type: string
  relations:
    - id: records_rows
      source:
        kind: api_read
        read_id: records
        applied_filters:
          - field_ids: [data_group_label]
            operator: equals
            value_id: alpha
      fields:
        - field_id: data_group_label
          roles: [output]
  values:
    - id: alpha
      kind: named
      text: Alpha
expect:
  result_contains:
    endpoint_args: []
    filters:
      - input_relation: records_rows__source
        output_relation: records_rows
        field_id: data_group_label
        operator: equals
        value_ref: parameter:value.alpha
        proof_refs: []
