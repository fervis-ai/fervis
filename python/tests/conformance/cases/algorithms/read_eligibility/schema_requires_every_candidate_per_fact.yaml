id: schema_requires_every_candidate_per_fact
description: Schema requires every shown candidate to be reviewed for each requested fact.
migrates:
  - api/tests/fervis/lookup/test_read_eligibility_contract.py::test_read_eligibility_schema_requires_every_shown_candidate_for_each_fact
kind: algorithm
algorithm: read_eligibility.schema_validate
input:
  request_fixture: sales_count
  payload:
    requested_fact_assessments:
      - requested_fact_id: fact_1
        read_candidate_reviews:
          - source_candidate_id: source_1
            read_id: list_sales_summary
            relevant_row_path_tokens: [source_1.row.root]
            relevant_field_tokens: [source_1.field.data.count]
            retention_basis: The read exposes sales count evidence.
            retention_decision: RETAIN
expect:
  error_contains: is too short
